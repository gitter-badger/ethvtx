import { Dispatch } from 'redux';
/**
 * @desc This methods will tell the redux store to fetch the given block height
 * @param dispatch
 * @param height
 */
export declare const fetchBlock: (dispatch: Dispatch<import("redux").AnyAction>, height: number) => void;
